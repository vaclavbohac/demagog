<article class="container-page-show">
  <h1><%= @page.title %></h1>

  <section class="segment">
    <%= raw(@page.text_html.gsub('<p>[darujme_widget]</p>', render('page/parts/darujme_widget'))) %>
  </section>
</article>
