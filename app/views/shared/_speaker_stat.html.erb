<a class="container-speaker-stat" href="<%= speaker_path(speaker.id) %>">
  <%= render(partial: 'shared/portrait', object: speaker, as: :speaker) %>
  <div class="name-and-stats">
    <p class="name"><%= speaker.full_name %></p>
    <%= render(partial: 'shared/stats', object: stats, as: :stats) %>
  </div>
</a>
