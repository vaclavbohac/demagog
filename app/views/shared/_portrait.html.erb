<% if speaker.avatar.attached? %>
  <figure class="portrait">
    <% if local_assigns[:lazy_load_images] == false %>
      <img class="lazy-load" data-src="<%= speaker.avatar.url %>" alt="<%= speaker.full_name %>">
    <% else %>
      <img src="<%= speaker.avatar.url %>" alt="<%= speaker.full_name %>" />
    <% end %>
  </figure>
<% end %>
